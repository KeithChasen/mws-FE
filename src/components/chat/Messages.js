import React from 'react';
import { ChatMessagesHolder } from "../../elements/chat/wrapper";

const Messages = () => {
  return (
    <ChatMessagesHolder>

    </ChatMessagesHolder>
  );
};

export default Messages;
